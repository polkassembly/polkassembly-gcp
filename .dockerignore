# Ignore development artifacts
node_modules
npm-debug.log
.idea
.vscode

# Ignore version control
.git
.gitignore

# IMPORTANT: Do NOT ignore the firebase key file.
# The '!' explicitly un-ignores the file if a broader rule might catch it.
!firebase-key.json

# Optionally, if you have a general rule to ignore all JSON,
# the exception above will override it for this specific file.
# Example of a general rule you might have:
# *.json
